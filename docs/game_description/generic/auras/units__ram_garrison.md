# units/ram_garrison

Available to **15** civilisations. Generic aura of 0 A.D. 0.28.0 — see `docs/game_description/generic/auras/README.md` for the method.

Data file: `simulation/data/auras/units/ram_garrison.json`.

## Basic stats

- **Name:** Battering Team
- **Type:** garrison (stackable)
- **Affects:** Ram
- **Affected players:** Player, MutualAlly
- **Description:** Battering Rams +0.15 speed for each garrisoned soldier.
- **Modifications:**
  - +0.15 UnitMotion/WalkSpeed

## Civilisations

- **athen** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
- **brit** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
- **cart** — `template_unit_champion_infantry` (all champion infantry), `template_unit_infantry` (all infantry)
- **gaul** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
- **germ** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
- **han** — `template_unit_champion_infantry` (all champion infantry), `template_unit_infantry` (all infantry)
- **iber** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
- **kush** — `template_unit_champion_infantry` (all champion infantry), `template_unit_infantry` (all infantry)
- **mace** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
- **maur** — `template_unit_champion_infantry` (all champion infantry), `template_unit_infantry` (all infantry)
- **pers** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
- **ptol** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
- **rome** — `template_unit_champion_infantry` (all champion infantry), `template_unit_infantry` (all infantry)
- **sele** — `template_unit_champion_infantry` (all champion infantry), `template_unit_infantry` (all infantry)
- **spart** — `template_unit_champion_infantry` (all champion infantry), `template_unit_hero_infantry` (all heroes), `template_unit_infantry` (all infantry)
