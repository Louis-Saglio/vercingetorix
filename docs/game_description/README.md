# Game description — 0 A.D. 0.28.0

Reference documentation of the game for agents developing the bot. Everything
here is grounded in the pinned game copy at `/home/ubuntu/0ad-reference/`
(0.28.0 — the version the harness runs), not in general knowledge of 0 A.D.:
entity data was extracted from the simulation templates by the versioned
scripts in `tools/`, and the mechanics files were written by reading the
simulation source, with inline `path:line` citations. If a statement looks
wrong, trust the source and fix the doc.

## Layout

- [`mechaniques/`](mechaniques/) — **how the game works**, one file per
  mechanic: resources/gathering, construction, training queues, population,
  combat/damage formula, capture, promotion/XP, territory, technologies and
  the modification pipeline, auras, vision/fog of war, garrisoning, healing,
  trade, loot, victory conditions, orders and simulation time. Start here to
  understand a rule of the game.
- [`generic/`](generic/) — **entity data** for units, buildings, technologies
  and auras shared by several civilisations (`units/`, `buildings/`,
  `technologies/`, `auras/`, one file per entity). Each file gives the merged
  template stats, per-civ variants, and which civilisations can train/build/
  research it.
- [`gauls/`](gauls/), [`romans/`](romans/) — the same for entities exclusive
  to one civilisation (the bot plays **gaul**, the opponent **rome**).

Every unit and building file opens with a **Guide** section: what the entity
is for and its role in the game, from a bot's perspective (resource scarcity
framing: food/wood are abundant, stone/metal are premium).

## How to use this when writing bot code

1. Strategy-level facts (civs, economy model, counters, phases):
   [`docs/GAME.md`](../GAME.md).
2. A rule you are unsure about (e.g. "how does capture actually work?",
   "when does my command take effect?"): the matching file in `mechaniques/`.
3. Concrete stats of an entity (cost, attack, armor, counters, trainer list):
   its file in `generic/` or `<civ>/`.
4. How to read state and issue commands: the AI API reference in
   [`docs/DEVELOPER_GUIDE.md`](../DEVELOPER_GUIDE.md).

## Regenerating the entity docs

The `generic/` and `<civ>/` files are generated by `tools/analyze.py`,
`tools/buildings.py`, `tools/technologies.py`, `tools/auras.py` and
`tools/civ.py` — see `tools/README.md`. Warning: regeneration overwrites the
whole file, including the handwritten **Guide** sections — re-apply or
restore them after copying fresh output over the docs. The `mechaniques/`
files are fully handwritten and never generated.
