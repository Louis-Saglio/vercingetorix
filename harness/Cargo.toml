[package]
name = "harness"
version = "0.1.0"
edition = "2021"

[dependencies]
# serde/serde_json: the engine emits statistics as JSON on stdout; parsing it
# without a JSON parser would mean reimplementing one — not justifiable.
serde = { version = "1", features = ["derive"] }
serde_json = "1"
